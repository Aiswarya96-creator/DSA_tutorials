# python: data types,variables,list
.py file extension is saved 
Ctrl / for hash
Variables:reserved memory location to store values.
A variable name must start with a letter or underscore character

Use lowercase letters with words seperated by underscore.

To collect from user and store in the variable name we use" input"

Data types: strings,tuples,numbers,list,dictionary,sets,boolean

Python string methods:capitalize(),lower(),upper(),count(),center(),replace(),


Is in to check if words are present or not
Not in to check if the words are not present 

List-mutable(changeable), tuple-unmutable, set-mutable, dictionary
