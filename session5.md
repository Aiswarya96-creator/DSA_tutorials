# Virtual environment
Python 3.13.13. download
Snake case used  becuz underscore is used in folder to save. Camel case is also there but we don’t use that as hyphen is used which can problem further.
In terminal or by typing cmd python -V for checking downloaded and then type python 
Then we saved a hello world folder by hello_world.py (in notepad we did this as In it got autuoamtically saved in folder)and in the folder we searched in top downloads cmd to open the terminal and that way the terminal got open and we typed hello_world.py we entered in the downloads search dir this gives all the downloaded things in the file.if we do in power shell then ls is needed to be typed so it shows the entire file
But we usually do everything in cmd. After dir is given we download pip version that is:python -m pip --version 
python -m pip install --upgrade pip.these 2 are used to download.

Now about vs code :we create a folder called code and gives enter again another folder is created in it that is hello world and we search the path at the top with cmd the terminal gets opened type (code .) for the vs code to open in hello world.the main.py is created to run the code.there we wrote print("helllo world")to open the output terminal.

In vs code open settings to tick format save and autosave-onwindowchange
Pylance type checking basic mode is given.
To create a virtual environment we do it in venv
Interactive python notebook-ipynb
